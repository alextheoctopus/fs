**StorageBenchmark.kt**
#### Кастомный бенч для измерения слоя хранения
* Redis хранилище:
→ saveAll(...)
→ getPayloads(...)
* Насколько быстро Redis работает как storage backend для моего API хранения
